'use client';
import {useMemo,useState} from 'react';

type Item={id:number;name:string;qty:number;unit:string;cost:number};
const seed:Item[]=[{id:1,name:'Resina Epóxi',qty:25,unit:'kg',cost:18},{id:2,name:'Endurecedor',qty:12,unit:'kg',cost:14},{id:3,name:'Primário',qty:18,unit:'kg',cost:11}];
export default function Operacao(){
 const [tab,setTab]=useState('Orçamento'),[items,setItems]=useState(seed),[hours,setHours]=useState(32),[rate,setRate]=useState(15),[area,setArea]=useState(120),[price,setPrice]=useState(7200),[workers,setWorkers]=useState(2);
 const materialCost=useMemo(()=>items.reduce((s,i)=>s+i.qty*i.cost,0),[items]); const labour=hours*rate*workers; const total=materialCost+labour; const profit=price-total; const margin=price?profit/price*100:0;
 function add(){setItems(x=>[...x,{id:Date.now(),name:'Novo material',qty:1,unit:'kg',cost:10}])}
 return <main style={{fontFamily:'Arial',padding:24,maxWidth:1250,margin:'auto'}}><h1>Royal Epoxi — Operação</h1><p>Gestão integrada de orçamento, obra, materiais e rentabilidade.</p><nav style={{display:'flex',gap:8,flexWrap:'wrap',margin:'20px 0'}}>{['Orçamento','Medições','Materiais','Mão de obra','Caixa','Relatórios'].map(x=><button key={x} onClick={()=>setTab(x)} style={{padding:'10px 14px',fontWeight:tab===x?'bold':'normal'}}>{x}</button>)}</nav>{tab==='Orçamento'&&<><h2>Novo orçamento</h2><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(180px,1fr))',gap:12}}><label>Área (m²)<input type="number" value={area} onChange={e=>setArea(+e.target.value)} style={{display:'block',width:'100%',padding:8}}/></label><label>Preço cliente (€)<input type="number" value={price} onChange={e=>setPrice(+e.target.value)} style={{display:'block',width:'100%',padding:8}}/></label><label>Trabalhadores<input type="number" value={workers} onChange={e=>setWorkers(+e.target.value)} style={{display:'block',width:'100%',padding:8}}/></label><label>Horas totais<input type="number" value={hours} onChange={e=>setHours(+e.target.value)} style={{display:'block',width:'100%',padding:8}}/></label><label>Valor/hora<input type="number" value={rate} onChange={e=>setRate(+e.target.value)} style={{display:'block',width:'100%',padding:8}}/></label></div><h3>Materiais</h3>{items.map((i,n)=><div key={i.id} style={{display:'grid',gridTemplateColumns:'2fr 1fr 1fr 1fr',gap:8,marginBottom:8}}><input value={i.name} onChange={e=>setItems(a=>a.map((z,k)=>k===n?{...z,name:e.target.value}:z))}/><input type="number" value={i.qty} onChange={e=>setItems(a=>a.map((z,k)=>k===n?{...z,qty:+e.target.value}:z))}/><input value={i.unit}/><input type="number" value={i.cost} onChange={e=>setItems(a=>a.map((z,k)=>k===n?{...z,cost:+e.target.value}:z))}/></div>)}<button onClick={add}>+ Material</button><section style={{marginTop:25,display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(170px,1fr))',gap:12}}>{[['Área',`${area} m²`],['Materiais',`€${materialCost.toFixed(0)}`],['Mão de obra',`€${labour.toFixed(0)}`],['Custo total',`€${total.toFixed(0)}`],['Lucro',`€${profit.toFixed(0)}`],['Margem',`${margin.toFixed(1)}%`]].map(([a,b])=><div style={{padding:18,border:'1px solid #ddd',borderRadius:12}} key={a}><small>{a}</small><h2>{b}</h2></div>)}</section><button style={{marginTop:20,padding:12}} onClick={()=>window.print()}>Imprimir orçamento</button></>}{tab==='Medições'&&<><h2>Visita & Medições</h2><p>Registar áreas, zonas e alterações de medidas durante a obra.</p><label>Área total (m²)<input type="number" value={area} onChange={e=>setArea(+e.target.value)}/></label><p>Consumo estimado por área pode ser recalculado antes da aprovação final.</p></>}{tab==='Materiais'&&<><h2>Materiais / Stock</h2>{items.map(i=><p key={i.id}><strong>{i.name}</strong> — {i.qty} {i.unit} × €{i.cost}</p>)}<button onClick={add}>+ Material</button></>}{tab==='Mão de obra'&&<><h2>Mão de obra</h2><p>Equipa: {workers} trabalhadores · {hours} horas · €{rate}/hora</p><h2>€{labour.toFixed(2)}</h2></>}{tab==='Caixa'&&<><h2>Folha de Caixa</h2><p>Entradas e saídas devem ser registadas por data, categoria, obra e método de pagamento.</p><h2>Saldo operacional: €{profit.toFixed(2)}</h2></>}{tab==='Relatórios'&&<><h2>Relatório da obra</h2><p>Faturação €{price.toFixed(2)} · Custos €{total.toFixed(2)} · Lucro €{profit.toFixed(2)} · Margem {margin.toFixed(1)}%</p></>}</main>
}
